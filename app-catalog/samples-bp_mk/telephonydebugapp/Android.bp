android_app {
    name: "TelephonyDebugAppExt",

    certificate: "platform",
    platform_apis: true,
    system_ext_specific: true,

    optimize: {
        enabled: false,
    },

    // Use System - Settings libs.
    defaults: ["SettingsLibDefaults"],

    libs: [
        "telephony-common",
        "extphonelib",
    ],

    srcs: ["src/**/*.java"],
}
